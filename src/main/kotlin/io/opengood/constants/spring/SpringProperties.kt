package io.opengood.constants.spring

class SpringProperties {
    companion object {
        const val APPLICATION_NAME = "spring.application.name"
        const val DATA_MONGODB_DATABASE = "spring.data.mongodb.database"
        const val DATA_MONGODB_HOST = "spring.data.mongodb.host"
        const val DATA_MONGODB_PASSWORD = "spring.data.mongodb.password"
        const val DATA_MONGODB_PORT = "spring.data.mongodb.port"
        const val DATA_MONGODB_URI = "spring.data.mongodb.uri"
        const val DATA_MONGODB_USERNAME = "spring.data.mongodb.username"
    }
}
